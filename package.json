{
  "name": "neumact-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-contract.mjs && astro build && node scripts/gen-sitemap.mjs",
    "preview": "astro preview",
    "check:contract": "node scripts/check-contract.mjs"
  },
  "dependencies": {
    "astro": "^4.15.0"
  }
}
